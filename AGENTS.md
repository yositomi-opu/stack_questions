# 開発エージェント向けルール

## 作業の開始

- まずルートの `HANDOFF.md` と、変更対象のREADMEを読む。
- 作業ディレクトリ、ブランチ、未コミット変更を確認する。他の作業者や利用者の変更を上書きしない。
- Nextcloudなどの同期フォルダ内のコピーを開発用とせず、同期対象外のcloneを使う。

## 引き継ぎの維持

- 開発作業の終了時、または別の環境へ引き継ぐ前に、ルートの `HANDOFF.md` を更新する。
- 実装内容・決定事項・検証結果・未確認事項・次の作業を実際の状態に合わせて記録する。変更がない会話だけのターンでは不要。
- テスト成功と実機確認を区別し、未実施の確認を完了と書かない。
- コードをコミット・pushする際には、対応する引き継ぎメモも含める。未送信の変更をpush済みと記載しない。
- メモはGitHubで公開される。個人の絶対パス、認証情報、非公開データは含めず、リポジトリ相対パスを使う。

## 変更と検証

- UIの文言を変更するときは、日本語・英語の切り替えも考慮する。
- 既存のCSV互換性やXML生成を保ち、変更に応じた検証を行う。実施コマンドと結果を引き継ぎメモへ残す。
- 各Mac固有の設定・生成キャッシュをGitへ追加しない。
- 利用者の明示的な依頼なしに、変更を失うGit操作や強制pushを行わない。

## 翻訳と公開ドキュメント

- 翻訳では人が読む文章だけを対象とし、変数名・関数名の大文字小文字、数式、LaTeX、HTML、STACKの埋め込み構文、プレースホルダーを保持する。プレースホルダーを無断で追加・変更しない。
- READMEは初めて利用する人向けに書く。個別の修正依頼、会話内だけで通じる変数名の例、AI向けの指示は載せない。継続的な開発ルールはこのファイル、開発状況はHANDOFF.mdへ記載する。
