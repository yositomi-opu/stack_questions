/* This file is flag setting part of 001.MCQ-rb/cb.xml for debug use */
/* The difference of -cb and -rb is only type of ans1(checkbox or radio button) and this setting */
%__mcq_rb_cb:"rb";
/*************** some usual fuctions I use. Feel free to customize here **********************/
powerdisp:true;
%_rp(L):=random_permutation(L);
tex2(m):=sconcat("\\(", tex1(m), "\\)");
texP1(a,u):=sconcat("\\(", tex1(a), "+p", tex1(u), "\\quad (p\\in{\\mathbb R})\\)");
texP2(a,u,v):=sconcat("\\(", tex1(a), "+p", tex1(u), "+q", tex1(v), "\\quad (p,q\\in{\\mathbb R})\\)");
/*********************************** Settings ***********************************/
/* NOTE: opts=options, copts=correct options, wopts=wrong(incorrect) options */
/* Number of all options and correct options (rb/radio button case admit does work) */
/* If the number of all options (or the sum of options) for each pattern is less than this setting, */
/* these values will be automatically adjusted. _wopts=_opts-_copts */
%__mcq_num_opts:4;
%__mcq_num_copts:1;
/* Flags : modification not required if not necessary. See mcq_template_pre.txt for details */
/* [use feedback?, use subscore?, use noidea opt, use nocorrect opt, is nocorrect opt true?, use qtext_auto?] */
%_MCQ_FLAGS:[true, true, false, false, rand([true, false]), true]; 
/* NOTE: If the number of correct/incorrect answer patterns exceeds 5/9, increase the number of PRT nodes */
/*       and adjust the following constants. */
%__mcq_max_cp:5;
%__mcq_max_wp:9;
/*****************************************************************************************/
/**** You can make database using the area below until END of MAIN QUESTION VARIABLES ****/
/******************************** MAIN QUESTION VARIABLES ********************************/l
