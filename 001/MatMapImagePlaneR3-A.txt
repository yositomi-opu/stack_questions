/******************** MatMapImagePlaneR3-A (rb/cb) *********************/
/* 001.MCB include file		                                 */
/* Objective: To learn/check image of line in R2 by linear map   */
/*****************************************************************/
/* c:[3], w:[4, 6]; appropriate: 8/1 (or 2, 3)*/
if not numberp(%_MCQ_NUM_OPTS) then %_MCQ_NUM_OPTS:5;
if %_MCQ_NUM_OPTS>8 then %_MCQ_NUM_OPTS:8;
if not numberp(%_MCQ_NUM_COPTS) then %_MCQ_NUM_COPTS:1;
/********************************************************************/

/*** for 000.MCQ-rb.xml ***/
/* key parameters(e.g. which control the only level of the question) here if required */
/* The truth values of compound propositions. */
tex2(m):=sconcat("\\(", tex1(m), "\\)");
tex2L(L):=block([s:"\\(", k], for k:1 thru length(L) do s:sconcat(s, if stringp(L[k]) then L[k] else tex1(L[k]), " "), s:sconcat(s, "\\)"), return(s));
%_rp(L):=random_permutation(L);

abL:%_rp([-4, -3, -2, -1, -1, 1, 1, 2, 3, 4]);
aa:abs(abL[1]);
bb:abL[2];
cc:abL[3];
normalVecR:matrix([aa, bb, cc]);
direction_vectors:nullspace2(normalVecR);
%_nvR:normalVecR;
%_dv1:integer_vector(direction_vectors[1]);
%_dv2:integer_vector(direction_vectors[2]);

ListAL1:[];
while (length(ListAL1)<3) do (
      %_aa1:rand([-2, -1, -1, 1, 1, 2]),
      %_aa2:rand([-3, -2, -1, 0, 1, 2, 3]),
      ListAL1:unique(cons(addrow(%_nvR, %_aa1*%_nvR, %_aa2*%_nvR), ListAL1)));

/* number of rows and cols */
bL:make_random_distribution([[3, 1], [2, 2], [1, 20], [0, 5]]);
ListBL1:[];
while (length(ListBL1)<4) do ListBL1:unique(cons(random_invertible_matrix(3, bL), ListBL1));

ListBL2:[];
while (length(ListBL2)<2) do ListBL2:unique(cons(genmatrix(lambda([i, j], rand(abL)), 2, 3), ListBL2));
while (length(ListBL2)<4) do ListBL2:unique(cons(genmatrix(lambda([i, j], rand(abL)), 3, 2), ListBL2));
while (length(ListBL2)<6) do ListBL2:unique(cons(genmatrix(lambda([i, j], rand(abL)), 2, 2), ListBL2));

/* security check */
SetAL1:setify(ListAL1);
/* SetAL2:setify(ListAL2); */
SetBL1:setify(ListBL1);
SetBL2:setify(ListBL2);
/* SetBL3:setify(ListBL3); */
/* chkAA12:intersection(SetAL1,SetAL2); */
chkAB11:intersection(SetAL1,SetBL1);
chkAB12:intersection(SetAL1,SetBL2);
/* chkAB13:intersection(SetAL1,SetBL3); */
/* chkAB21:intersection(SetAL2,SetBL1); */
/* chkAB22:intersection(SetAL2,SetBL2); */
/* chkAB23:intersection(SetAL2,SetBL3); */
chkBB12:intersection(SetBL1,SetBL2);
/* chkBB13:intersection(SetBL1,SetBL3); */
/* chkBB23:intersection(SetBL2,SetBL3);*/
/* allchk:[chkAA12,chkAB11,chkAB12,chkAB13,chkAB21]; */
/* allchk:append(allchk,[chkAB22,chkAB23,chkBB12,chkBB13,chkBB23]);*/
allchk:[chkAB11,chkAB12,chkBB12];
/* allchk:[chkAB11]; */

PlanEq:sconcat("\\(", display_polynomial_varlist_tex1(aa*x+bb*y+cc*z, [x, y, z]), "=", tex1(rand(9)-4), "\\)");

/********************************** Question text ****************************************/
/* %__mcq_qtextL : question text language list                                           */
/* NOTE: In the question text, string "__SELTYPE__" is replaced automatically.           */
/*****************************************************************************************/
/* if not depend on locale, define '%__mcq_qtext'(may include __SELTYPE__) or 'qtext' directly */
%__mcq_qtextL:[["en", sconcat(__SELTYPE__, " matrices among the following that define a linear transformation of space and map the plane ", PlaneEq, " to a single point")],
 ["fr", sconcat(__SELTYPE__, " matrices parmi les suivantes qui définissent une transformation linéaire de l’espace et qui envoient le plan ", PlaneEq, " sur un point unique")],
 ["de", sconcat(__SELTYPE__, " Matrizen unter den folgenden, die eine lineare Transformation des Raums definieren und die Ebene ", PlaneEq, " auf einen einzigen Punkt abbilden")],
 ["it", sconcat(__SELTYPE__, " matrici tra le seguenti che definiscono una trasformazione lineare dello spazio e che mandano il piano ", PlaneEq, " in un unico punto")],
 ["ja", sconcat("次の行列のうち, 空間の1次変換を定め, かつ平面", PlaneEq, "が1点に写るようなものを __SELTYPE__ ")]];

/******************************** List of patterns/feedbacks *****************************/
/*  This template assumes the lists %__Copt?L, %__Wopt?L does not depend on the locale.  */
/*  If it depends on the locale, please set up the list using %_STACK_LANG.              */
/*****************************************************************************************/
/* Correct pattern 1 */
%__CoptL1:map(tex2, ListAL1);
%__Cmsg1L:[["en", "Correct. Under a linear transformation, a plane is mapped to a plane, but if two vectors parallel to the plane collapse to the zero vector, it can be mapped to a single point."],
 ["fr", "Correct. Par une transformation linéaire, un plan est envoyé sur un plan, mais si deux vecteurs parallèles au plan sont réduits au vecteur nul, il peut être envoyé sur un seul point."],
 ["de", "Richtig. Unter einer linearen Transformation wird eine Ebene auf eine Ebene abgebildet, aber wenn zwei zur Ebene parallele Vektoren zum Nullvektor werden, kann sie auf einen einzigen Punkt abgebildet werden."],
 ["it", "Corretto. Con una trasformazione lineare, un piano viene mandato su un piano, ma se due vettori paralleli al piano si annullano diventando il vettore nullo, può essere mandato in un unico punto."],
 ["ja", "正解です. 空間の線形変換によって基本平面は平面に写りますが, 2つの平面に平行なベクトルが零ベクトルに潰れてしまうことで1点に写ることもありますね."]];

/* Wrong answer pattern 1 */
%__WoptL1:map(tex2, ListBL1);
%__Wmsg1L:[["en", "Under a linear transformation, a plane is mapped to a plane, but what happens if the images of two vectors parallel to the plane are the zero vector?"],
 ["fr", "Par une transformation linéaire, un plan est envoyé sur un plan, mais que se passe-t-il si les images de deux vecteurs parallèles au plan sont le vecteur nul ?"],
 ["de", "Unter einer linearen Transformation wird eine Ebene auf eine Ebene abgebildet, aber was passiert, wenn die Bilder von zwei zur Ebene parallelen Vektoren der Nullvektor sind?"],
 ["it", "Con una trasformazione lineare, un piano viene mandato su un piano, ma cosa succede se le immagini di due vettori paralleli al piano sono il vettore nullo?"],
 ["ja", "線形変換によって基本平面は平面に写りますが, 2つの平面に平行なベクトルの写り先(像)が零ベクトルだとどうなりますか？"]];

/* Wrong answer pattern 2 */
%__WoptL2:map(tex2, ListBL2);
%__Wmsg2L:[["en", "It seems you have selected matrices that do not define a linear transformation of space. Let’s review the definition of the mapping determined by a matrix."],
 ["fr", "Il semble que vous ayez choisi des matrices qui ne définissent pas une transformation linéaire de l’espace. Vérifions la définition de l’application déterminée par une matrice."],
 ["de", "Es scheint, dass Sie Matrizen ausgewählt haben, die keine lineare Transformation des Raums definieren. Überprüfen wir die Definition der durch eine Matrix bestimmten Abbildung."],
 ["it", "Sembra che tu abbia scelto matrici che non definiscono una trasformazione lineare dello spazio. Verifichiamo la definizione dell’applicazione determinata da una matrice."],
 ["ja", "そもそも空間の1次変換を定めない行列を選んでいるようです. 行列の定める写像の定義から確認しよう."]];


/********** EOF **********/
