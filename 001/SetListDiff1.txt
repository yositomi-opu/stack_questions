/* For 001.MCQ-cb : Difference of Set and List(permutation) */

SS:%_rp(makelist(i,i,-8,8));
nm:rand([4, 5]);
/* do not use setify because we do not want to reorder!! */
CtoS(L):=sconcat("\\(", ssubst("\\{", "[", ssubst("\\}", "]", tex1(L))), "\\)");
C0:sort(makelist(SS[i], i, 1, nm)); /* list for target set */
S0:CtoS(C0);
C1:%_rp(makelist(SS[i], i, 1, nm));
C2:%_rp(append(C0, makelist(SS[rand(nm)+1], k, 1, 1)));
C3:%_rp(append(C0, makelist(SS[rand(nm)+1], k, 1, 2)));
C4:%_rp(append(C0, makelist(SS[rand(nm)+1], k, 1, rand([3, 4]))));
W1:%_rp(append(C0, makelist(SS[nm+1+rand(5)], k, 1, 1)));
W2:%_rp(append(C0, makelist(SS[nm+1+rand(5)], k, 1, 2)));
W3:%_rp(makelist(SS[i], i, 2, nm));
W4:%_rp(makelist(SS[i], i, 1, nm-1));
W5:%_rp(append(W3, makelist(SS[nm+1+rand(5)], k, 1, rand([2,3,4]))));
W6:%_rp(append(W4, makelist(SS[nm+1+rand(5)], i, 1, rand([2,3,4]))));
/********************************** Question text ****************************************/
/* %__mcq_qtextL : question text language list                                           */
/* NOTE: In the question text, string "__SELTYPE__" is replaced automatically.           */
/*****************************************************************************************/
/* if not depend on locale, define '%__mcq_qtext'(may include __SELTYPE__) or 'qtext' directly */
%__mcq_qtextL:[["en", sconcat("Select __SELTYPE__ item(s) that are equal to the set ", S0, ".")],
  ["fr", sconcat("Sélectionnez __SELTYPE__ élément(s) qui sont égaux à l’ensemble ", S0, ".")],
  ["de", sconcat("Wählen Sie __SELTYPE__ Element(e) aus, die gleich der Menge ", S0 ,"sind."),]
  ["ja", sconcat("集合", S0, "と同じになるものを __SELTYPE__ 選びなさい. ")]];
/******************************** List of patterns/feedbacks *****************************/
/*  This template assumes the lists %__Copt?L, %__Wopt?L does not depend on the locale.  */
/*  If it depends on the locale, please set up the list using %_STACK_LANG.              */
/*****************************************************************************************/
/* Correct pattern 1 */
%__CoptL1:[CtoS(C1), CtoS(C2), CtoS(C3), CtoS(C4)];
%__Cmsg1L:[["en", "They match as sets when order and duplicates are ignored."],
 ["fr", "Ils correspondent en tant qu’ensembles lorsque l’ordre et les doublons sont ignorés."],
 ["de", "Sie stimmen als Mengen überein, wenn Reihenfolge und Duplikate ignoriert werden."],
 ["ja", "順序と重複を無視して一致しているものが集合として一致していますね."]];
%__WoptL1:[CtoS(W1), CtoS(W2), CtoS(W3), CtoS(W4), CtoS(W5), CtoS(W6)];
%__Wmsg1L:[["en", "You have selected those that do not match even when duplicates and order are ignored."],
 ["fr", "Vous avez sélectionné ceux qui ne correspondent pas, même en ignorant les doublons et l’ordre."],
 ["de", "Sie haben diejenigen ausgewählt, die selbst bei Ignorieren von Duplikaten und Reihenfolge nicht übereinstimmen."],
 ["ja", "重複と順序を除いても一致していないものを選択しています."]];

/********** EOF ***********/

