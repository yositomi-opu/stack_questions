/******************** MatMapImagePlaneR3-B (rb) ******************/
/* 001.MCB include file		                                 */
/* Objective: To learn/check image of plane in R3 by linear map  */
/*****************************************************************/
/* c:[3], w:[6]; appropriate: 5/1 */
if not numberp(%_MCQ_NUM_OPTS) then %_MCQ_NUM_OPTS:5;
if %_MCQ_NUM_OPTS>8 then %_MCQ_NUM_OPTS:8;
if not numberp(%_MCQ_NUM_COPTS) then %_MCQ_NUM_COPTS:1;
/********************************************************************/

/*** for 000.MCQ-rb.xml ***/
/* key parameters(e.g. which control the only level of the question) here if required */
/* The truth values of compound propositions. */
tex2(m):=sconcat("\\(", tex1(m), "\\)");
tex2L(L):=block([s:"\\(", k], for k:1 thru length(L) do s:sconcat(s, if stringp(L[k]) then L[k] else tex1(L[k]), " "), s:sconcat(s, "\\)"), return(s));
%_rp(L):=random_permutation(L);

pL:make_random_distribution_pm([[1, 10], [2, 3], [3, 1]]);
matP:random_invertible_matrix(3, pL);
%_f1:rand([-3, -2, -1, 1, 2, 3]);
%_f2:rand([-3, -2, -1, 1, 2, 3]);
matF:matrix([1, 0, %_f1], [0, 1, %_f2], [0, 0, 0]);
matA:matP.matF;
nullvec:transpose(matrix([-%_f1, -%_f2, 1]));

dirvecL:[];
while (length(dirvecL)<3) do (
  %_tmpv:genmatrix(lambda([i, j], rand(pL)), 3, 1),
  if rank(addcol(%_tmpv, nullvec))=2 then dirvecL:unique(cons(%_tmpv, dirvecL)));
normalvecL:makelist(integer_vector(cross_product(nullvec, dirvecL[k])), k, 1, length(dirvecL));

mkEq(v):=sconcat("\\(", display_polynomial_varlist_tex1(v.matrix([x], [y], [z]), [x, y, z]), "=", tex1(rand(9)-4), "\\)");
ListAL1:map(mkEq, normalvecL);


/* direction vectors for plane to plane case */
uvlist:[];
%_tmpgen(M):=block([uv], uv:genmatrix(lambda([i, j], rand(pL)), 3, 2), while (rank(M.uv)<2) do (
  uv:genmatrix(lambda([i, j], rand(pL)), 3, 2)), return(uv));
while (length(uvlist)<6) do (
  uvlist:unique(cons(%_tmpgen(matA), uvlist)));

ListBL1:makelist(mkEq(integer_vector(cross_product(col(uvlist[k], 1), col(uvlist[k], 2)))),
 k, 1, length(uvlist));
 


/* security check */
SetAL1:setify(ListAL1);
/* SetAL2:setify(ListAL2); */
SetBL1:setify(ListBL1);
/* SetBL2:setify(ListBL2); */
/* SetBL3:setify(ListBL3); */
/* chkAA12:intersection(SetAL1,SetAL2); */
chkAB11:intersection(SetAL1,SetBL1);
/* chkAB12:intersection(SetAL1,SetBL2); */
/* chkAB13:intersection(SetAL1,SetBL3); */
/* chkAB21:intersection(SetAL2,SetBL1); */
/* chkAB22:intersection(SetAL2,SetBL2); */
/* chkAB23:intersection(SetAL2,SetBL3); */
/* chkBB12:intersection(SetBL1,SetBL2);*/
/* chkBB13:intersection(SetBL1,SetBL3); */
/* chkBB23:intersection(SetBL2,SetBL3);*/
/* allchk:[chkAA12,chkAB11,chkAB12,chkAB13,chkAB21]; */
/* allchk:append(allchk,[chkAB22,chkAB23,chkBB12,chkBB13,chkBB23]);*/
/* allchk:[chkAB11,chkAB12,chkBB12]; */
allchk:[chkAB11];

EqZ:"\\(={\\boldsymbol0}\\}";

/********************************** Question text ****************************************/
/* %__mcq_qtextL : question text language list                                           */
/* NOTE: In the question text, string "__SELTYPE__" is replaced automatically.           */
/*****************************************************************************************/
/* if not depend on locale, define '%__mcq_qtext'(may include __SELTYPE__) or 'qtext' directly */
%__mcq_qtextL:[["en", sconcat(__SELTYPE__, " planes that are mapped to a line by the linear transformation defined by the matrix ", tex2(matA), ". Here, ", tex2(matA), tex2(nullvec), EqZ)],
 ["fr", sconcat(__SELTYPE__, " plans qui sont envoyés sur une droite par la transformation linéaire définie par la matrice ", tex2(matA), ". Ici, ", tex2(matA), tex2(nullvec), EqZ)],
 ["de", sconcat(__SELTYPE__, " Ebenen, die durch die durch die Matrix ", tex2(matA), " definierte lineare Abbildung auf eine Gerade abgebildet werden. Dabei gilt ", tex2(matA), tex2(nullvec), EqZ)],
 ["it", sconcat(__SELTYPE__, " piani che vengono mandati su una retta dalla trasformazione lineare definita dalla matrice ", tex2(matA), ". Qui, ", tex2(matA), tex2(nullvec), EqZ)],
 ["ja", sconcat("行列", tex2(matA), "の定める1次変換によって, 直線に写される平面を __SELTYPE__ ただし, ",
   tex2(matA), tex2(nullvec), EqZ, "である.")]];

/******************************** List of patterns/feedbacks *****************************/
/*  This template assumes the lists %__Copt?L, %__Wopt?L does not depend on the locale.  */
/*  If it depends on the locale, please set up the list using %_STACK_LANG.              */
/*****************************************************************************************/
/* Correct pattern 1 */
%__CoptL1:ListAL1;
%__Cmsg1L:[["en", "Correct. Under a linear transformation of space, basic planes are mapped to planes; however, if vectors parallel to the two planes are mapped to parallel vectors or if one of them becomes zero, the image becomes a line."],
 ["fr", "Correct. Sous une transformation linéaire de l’espace, les plans de base sont envoyés sur des plans ; cependant, si des vecteurs parallèles aux deux plans sont envoyés sur des vecteurs parallèles ou si l’un d’eux devient nul, l’image devient une droite."],
 ["de", "Richtig. Unter einer linearen Abbildung des Raums werden grundlegende Ebenen auf Ebenen abgebildet; wenn jedoch zu zwei Ebenen parallele Vektoren auf parallele Vektoren abgebildet werden oder einer von ihnen null wird, wird das Bild zu einer Geraden."],
 ["it", "Corretto. Sotto una trasformazione lineare dello spazio, i piani di base vengono mandati in piani; tuttavia, se vettori paralleli ai due piani vengono mandati in vettori paralleli oppure uno di essi diventa nullo, l’immagine diventa una retta."],
 ["ja", "正解です. 空間の線形変換によって基本平面は平面に写りますが, 2つの平面に平行なベクトルが平行なベクトルに写ったり一方が零になると直線になるわけですね."]];

/* Wrong answer pattern 1 */
%__WoptL1:ListBL1;
%__Wmsg1L:[["en", "The given matrix maps only one direction to zero. If that direction is parallel to a plane, then one of the parameters in the parametric representation of that plane becomes zero, and it is mapped to a line."],
 ["fr", "La matrice donnée n’envoie qu’une seule direction sur zéro. Si cette direction est parallèle à un plan, alors l’un des paramètres de la représentation paramétrique de ce plan devient nul et il est envoyé sur une droite."],
 ["de", "Die gegebene Matrix bildet nur eine Richtung auf null ab. Wenn diese Richtung zu einer Ebene parallel ist, wird einer der Parameter in der Parameterdarstellung dieser Ebene null, und sie wird auf eine Gerade abgebildet."],
 ["it", "La matrice data manda a zero una sola direzione. Se tale direzione è parallela a un piano, allora uno dei parametri nella rappresentazione parametrica di quel piano diventa nullo e l’immagine diventa una retta."],
 ["ja", "与えられた行列は1方向だけを零に写します. その方向がある平面に平行だと, その平面のパラメータ表示のうち1つが零になり, 直線に写ることになります."]];


/********** EOF **********/
