/*********************** SpaceLineSelPtsPara (cb  *******************/
/* 001.MCB include file		                                    */
/* Objective: To learn paratrization of line given parametrization  */
/********************************************************************/
/* c:[5], w:[5]; */
if not numberp(%_MCQ_NUM_OPTS) then %_MCQ_NUM_OPTS:5;
if not numberp(%_MCQ_NUM_COPTS) then %_MCQ_NUM_COPTS:1;
/*******************************************/
tex2(m):=sconcat("\\(", tex1(m), "\\)");
tex2L(L):=block([s:"\\(", k], for k:1 thru length(L) do s:sconcat(s, if stringp(L[k]) then L[k] else tex1(L[k]), " "), s:sconcat(s, "\\)"), return(s));
matP:random_invertible_matrix(3, [-3, -2, -1, -1, -1, 1, 1, 1, 2]);

pp:col(matP, 1);
vv:col(matP, 2);
ww:col(matP, 3);

ParaEq:tex2L([pp, "+p", vv]);
CL:makelist(pp+k*vv, k, 1, 3);
CL:append(CL, makelist(pp-k*vv, k, 1, 3));
WL:[2*pp, 2*pp+vv, 2*pp+2*vv];
WL:append(WL, makelist(-pp+k*vv, k, 0, 2));
WL:append(WL, [ww, vv, -vv]);

/********************************** Question text ****************************************/
/* %__mcq_qtextL : question text language list                                           */
/* NOTE: In the question text, string "__SELTYPE__" is replaced automatically.           */
/*****************************************************************************************/
/* if not depend on locale, define '%__mcq_qtext'(may include __SELTYPE__) or 'qtext' directly */
%__mcq_qtextL:[["en", sconcat(__SELTYPE__, " points on the line with parametric representation ", ParaEq)],
 ["fr", sconcat(__SELTYPE__, " points appartenant à la droite de représentation paramétrique ", ParaEq)],
 ["de", sconcat(__SELTYPE__, " Punkte auf der Geraden mit der Parameterdarstellung ", ParaEq)],
 ["it", sconcat(__SELTYPE__, " punti sulla retta con rappresentazione parametrica ", ParaEq)],
 ["ja", sconcat("パラメータ表示", ParaEq, "を持つ直線上にある点を__SELTYPE__")]];
/******************************** List of patterns/feedbacks *****************************/
/*  This template assumes the lists %__Copt?L, %__Wopt?L does not depend on the locale.  */
/*  If it depends on the locale, please set up the list using %_STACK_LANG.              */
/*****************************************************************************************/
/* Correct pattern 1 */
%__CoptL1:CL;
%__Cmsg1L:[["en", "Correct. You can correctly determine whether it is another point on the line."],
 ["fr", "Correct. Vous savez correctement déterminer s’il s’agit d’un autre point de la droite."],
 ["de", "Richtig. Sie können korrekt feststellen, ob es sich um einen weiteren Punkt auf der Geraden handelt."],
 ["ja", "正解です。他の通る点であるかどうかがきちんと判別できています."]];
/* Wrong pattern 1 */
%__WoptL1:WL;
%__Wmsg1L:[["en", "You can determine it by finding a value of \\(t\\) from one coordinate and then checking whether the other coordinates also match."],
 ["fr", "Vous pouvez le déterminer en trouvant une valeur de \\(t\\) à partir d’une coordonnée, puis en vérifiant si les autres coordonnées correspondent également."],
 ["de", "Sie können dies bestimmen, indem Sie einen Wert von \\(t\\) aus einer Koordinate berechnen und dann überprüfen, ob die anderen Koordinaten ebenfalls übereinstimmen."],
 ["it", "Puoi determinarlo trovando un valore di \\(t\\) da una coordinata e verificando poi se anche le altre coordinate coincidono."],
 ["ja", "\\(t\\)をどれかの座標で1つ求めて, それで他の座標も一致するかで判断できますよ."]];

/********** EOF **********/
